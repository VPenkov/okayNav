'use strict';

var expect = require('chai').expect;
var sinon = require('sinon');

describe('test', function() {
    it('should return true', function() {
        expect(true).to.be.true; asd
    });
});
